const express=require('express'); const bcrypt=require('bcryptjs'); const prisma=require('../lib/prisma'); const {requireAuth,requireRole}=require('../middleware/auth'); const {layout,esc}=require('../views/layout'); const {ROLES,EXPENSE_TYPES}=require('../config/constants'); const audit=require('../services/auditService'); const router=express.Router();
router.use(requireAuth,requireRole('ADMIN','PARTNER','PROCESS_MANAGER'));
router.get('/',async(req,res)=>{ const [users,rules,flows,logs]=await Promise.all([prisma.user.findMany({orderBy:{id:'asc'}}),prisma.riskRule.findMany({orderBy:{id:'asc'}}),prisma.approvalFlow.findMany({orderBy:{id:'asc'}}),prisma.auditLog.findMany({include:{user:true},orderBy:{createdAt:'desc'},take:20})]); res.send(layout(req,'Yönetim',`<div class="card"><h1>Yönetici Paneli</h1><p class="muted">Onay akışları, risk kuralları ve sistem davranışı buradan yönetilir.</p></div><section class="card"><h2>Kullanıcılar</h2><table><tr><th>Ad</th><th>E-posta</th><th>Rol</th><th>Departman</th></tr>${users.map(u=>`<tr><td>${esc(u.name)}</td><td>${esc(u.email)}</td><td>${u.role}</td><td>${esc(u.department)}</td></tr>`).join('')}</table><h3>Yeni Kullanıcı</h3><form method="post" action="/admin/users"><div class="grid"><input name="name" placeholder="Ad Soyad" required><input name="email" type="email" placeholder="E-posta" required><input name="password" placeholder="Şifre" value="123456"><select name="role">${ROLES.map(r=>`<option>${r}</option>`).join('')}</select><input name="department" placeholder="Departman"><input name="title" placeholder="Ünvan"></div><button>Kullanıcı Ekle</button></form></section><section class="card"><h2>Risk Kuralları</h2><table><tr><th>Aktif</th><th>Kural</th><th>Risk</th><th>Aksiyon</th><th>Limit</th><th></th></tr>${rules.map(r=>`<tr><td>${r.enabled?'✓':'-'}</td><td>${esc(r.name)}</td><td>${r.riskLevel}</td><td>${r.action}</td><td>${r.countLimit||r.amountLimit||r.periodDays||''}</td><td><form method="post" action="/admin/rules/${r.id}/toggle"><button class="secondary">Aç/Kapat</button></form></td></tr>`).join('')}</table><p class="muted">Limitleri PostgreSQL üzerinde değiştirmek yerine sonraki ekranda düzenleme formu genişletilebilir. Temel aç/kapat hazır.</p></section><section class="card"><h2>Onay Akışları</h2><table><tr><th>Ad</th><th>Tür</th><th>Min</th><th>Max</th><th>Adımlar</th></tr>${flows.map(f=>`<tr><td>${esc(f.name)}</td><td>${esc(f.expenseType||'Tümü')}</td><td>${f.minAmount||''}</td><td>${f.maxAmount||''}</td><td><pre>${esc(JSON.stringify(f.steps,null,2))}</pre></td></tr>`).join('')}</table><h3>Yeni Basit Akış</h3><form method="post" action="/admin/flows"><div class="grid"><input name="name" placeholder="Akış adı" required><select name="expenseType"><option value="">Tüm masraflar</option>${EXPENSE_TYPES.map(x=>`<option>${x}</option>`).join('')}</select><input name="minAmount" type="number" step="0.01" placeholder="Min"><input name="maxAmount" type="number" step="0.01" placeholder="Max"><input name="steps" value='[{"type":"MANAGER","label":"Bağlı Müdür"},{"type":"ACCOUNTING","label":"Muhasebe"},{"type":"FINANCE","label":"Finans"}]'></div><button>Akış Ekle</button></form></section><section class="card"><h2>Son Loglar</h2>${logs.map(l=>`<div class="mail"><b>${esc(l.action)}</b><p>${esc(l.user?.name||'Sistem')} - ${l.createdAt.toLocaleString('tr-TR')}</p></div>`).join('')}</section>`)); });
router.post('/users',async(req,res)=>{ await prisma.user.create({data:{name:req.body.name,email:req.body.email,passwordHash:await bcrypt.hash(req.body.password||'123456',10),role:req.body.role,department:req.body.department||null,title:req.body.title||null}}); await audit.log(req.session.user.id,'USER_CREATED',{email:req.body.email}); res.redirect('/admin'); });
router.post('/rules/:id/toggle',async(req,res)=>{ const r=await prisma.riskRule.findUnique({where:{id:Number(req.params.id)}}); await prisma.riskRule.update({where:{id:r.id},data:{enabled:!r.enabled}}); await audit.log(req.session.user.id,'RULE_TOGGLED',{rule:r.code,enabled:!r.enabled}); res.redirect('/admin'); });
router.post('/flows',async(req,res)=>{ await prisma.approvalFlow.create({data:{name:req.body.name,expenseType:req.body.expenseType||null,minAmount:req.body.minAmount||null,maxAmount:req.body.maxAmount||null,steps:JSON.parse(req.body.steps)}}); await audit.log(req.session.user.id,'FLOW_CREATED',{name:req.body.name}); res.redirect('/admin'); });
module.exports=router;
